programa {
  funcao inicio() {
    real base,altura,area,perimetro
leia(altura)
		leia(base)
    area = base * altura
    perimetro = (base + base) + (altura + altura)

    escreva("a area é: ", area, " O perimetro é: ", perimetro)
  }
}
